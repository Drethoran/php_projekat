{% extends 'base.html.twig' %}

{% block title %}Magacin index{% endblock %}

{% block body %}
<!--<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="colapse navbar-collapse">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link"  href="{{ path('magacin_new') }}">Create new</a>
            </li>
        </ul>
    </div>
</nav>-->
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Naziv proizvoda</th>
                <th>Stopa PDV(%)</th>
                <th>Prodajna cena</th>
                <th>Valuta</th>
                <th>Opis</th>
            </tr>
        </thead>
        <tbody>
        {% for magacin in magacins %}
            <tr>
                <td>{{ magacin.id }}</td>
                <td>{{ magacin.nazivProizvoda }}</td>
                <td>{{ magacin.stopaPdv }}</td>
                <td>{{ magacin.prodajnaCena }}</td>
                <td>{{ magacin.valuta }}</td>
                <td>{{ magacin.opis }}</td>
                <td>
                    <a class="btn btn-danger" href="{{ path('magacin_show', {'id': magacin.id}) }}">show</a>
                    <a class="btn btn-primary" href="{{ path('magacin_edit', {'id': magacin.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
